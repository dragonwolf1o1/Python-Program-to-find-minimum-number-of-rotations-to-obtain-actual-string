string=input("Enter Original String: ")
re_string=input("Enter the left shift string: ")
for i in range(1,len(string)):
    a=re_string[len(re_string)-i:]
    b=re_string[0:len(re_string)-i]
    if a+b==string:
        print(i)
    else:
        continue
